name: Zig VOPR soaks

on:
  workflow_call:
    inputs:
      head_sha:
        description: Approved revision to qualify (empty uses the triggering revision)
        type: string
        default: ''
      qualification_only:
        description: Build and qualify the runner without starting full soaks
        type: boolean
        default: false
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:
    inputs:
      qualification_only:
        description: Run qualification without the full soak campaign
        type: boolean
        default: false
      histories:
        description: Histories per shard (0 uses scenario budgets)
        type: number
        default: 0
      require_seed:
        description: Require replay-compatible entries restored from an earlier run
        type: boolean
        default: false

      diagnostic_run:
        description: Inspect an earlier soak with its original executable (empty runs a new soak)
        type: string
        default: ''
      diagnostic_shard:
        description: Artifact name when inspecting an earlier run
        type: string
        default: ''
      diagnostic_trace:
        description: Trace filename when inspecting an earlier run
        type: string
        default: ''
      diagnostic_replays:
        description: Fresh process diagnostic replays, stopping at first failure (1-200)
        type: number
        default: 1

permissions:
  contents: read

defaults:
  run:
    # Explicit bash enables pipefail, including production soaks piped to tee.
    shell: bash

concurrency:
  # Approved PR callers all have github.ref == refs/heads/main. Give each
  # qualification its own group: cancel-in-progress:false still lets a newer
  # pending request replace an older pending request in the same group.
  # The caller already cancels superseded runs for the same PR. Keep scheduled
  # and manually dispatched soak campaigns serialized by ref as before.
  group: vopr-soak-${{ inputs.qualification_only && github.run_id || github.ref }}-${{ inputs.diagnostic_trace || 'campaign' }}
  cancel-in-progress: false

jobs:
  diagnose:
    if: inputs.diagnostic_run != ''
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/zig-vopr-replay-diagnostics.yml
    with:
      run_id: ${{ inputs.diagnostic_run }}
      shard_artifact: ${{ inputs.diagnostic_shard }}
      trace_name: ${{ inputs.diagnostic_trace }}
      replays: ${{ fromJSON(inputs.diagnostic_replays || '1') }}

  # Cold qualification and runner compilation have independent job budgets.
  # Bound concurrent work by cgroup memory (capped at 22 GiB), not one build job.
  # Dedicated cache variables keep setup actions from replacing these paths.
  qualify:
    if: inputs.diagnostic_run == ''
    runs-on: arc-antfly-heavy
    # The VOPR runtime roots compile under a bounded 24 GiB pod. General
    # secrets-test coverage belongs to zig-base; keep the focused secret VOPR
    # replay here without recompiling the large physical secret backend root.
    timeout-minutes: 120
    env:
      VOPR_LOCAL_CACHE_DIR: /mnt/cache/antfly/zig/vopr-soak/${{ github.run_id }}-${{ github.run_attempt }}-qualify
      VOPR_GLOBAL_CACHE_DIR: /mnt/cache/antfly/zig/zig-global
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true
      - name: Sample ARC runner disk use
        uses: ./.github/actions/runner-disk-audit
      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy
      - name: Install build tools
        run: sudo apt-get update && sudo apt-get install -y build-essential pkg-config ca-certificates git curl xz-utils file
      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31
        with:
          nix_path: nixpkgs=https://github.com/NixOS/nixpkgs/archive/${{ steps.toolchain.outputs.zig_nixpkgs_revision }}.tar.gz
      - name: Install glibc-linked Zig
        env:
          ZIG_VERSION: ${{ steps.toolchain.outputs.zig_version }}
          ZIG_NIX_ATTRIBUTE: ${{ steps.toolchain.outputs.zig_nix_attribute }}
        run: |
          zig_path="$(nix-build '<nixpkgs>' -A "$ZIG_NIX_ATTRIBUTE" --no-out-link)"
          test "$("$zig_path/bin/zig" version)" = "$ZIG_VERSION"
          compiler_description="$(file "$zig_path/bin/zig")"
          echo "$compiler_description"
          grep -q 'dynamically linked' <<<"$compiler_description"
          echo "$zig_path/bin" >> "$GITHUB_PATH"
      - name: Test soak orchestration
        run: |
          python3 -m unittest discover -s scripts/ci -p test_zig_vopr_soak.py
          python3 -m unittest discover -s scripts/ci -p test_zig_e2e_regression_loop.py
      - name: Audit replayable VOPR sources
        working-directory: zig
        run: bash "$GITHUB_WORKSPACE/scripts/ci/zig_vopr_qualify.sh" audit
      - name: Test production transport and runtime scheduling
        working-directory: zig
        run: bash "$GITHUB_WORKSPACE/scripts/ci/zig_vopr_qualify.sh" runtime
      - name: Retain qualification disk measurements
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-qualify-disk
          path: ${{ runner.temp }}/vopr-qualify-disk.json
          if-no-files-found: warn

  build:
    if: inputs.diagnostic_run == ''
    runs-on: arc-antfly-heavy
    timeout-minutes: 90
    outputs:
      binary_sha256: ${{ steps.binary.outputs.sha256 }}
    env:
      VOPR_LOCAL_CACHE_DIR: /mnt/cache/antfly/zig/vopr-soak/${{ github.run_id }}-${{ github.run_attempt }}-build
      VOPR_GLOBAL_CACHE_DIR: /mnt/cache/antfly/zig/zig-global
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true
      - name: Sample ARC runner disk use
        uses: ./.github/actions/runner-disk-audit
      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy
      - name: Install build tools
        run: sudo apt-get update && sudo apt-get install -y build-essential pkg-config ca-certificates git curl xz-utils file
      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31
        with:
          nix_path: nixpkgs=https://github.com/NixOS/nixpkgs/archive/${{ steps.toolchain.outputs.zig_nixpkgs_revision }}.tar.gz
      - name: Install glibc-linked Zig
        env:
          ZIG_VERSION: ${{ steps.toolchain.outputs.zig_version }}
          ZIG_NIX_ATTRIBUTE: ${{ steps.toolchain.outputs.zig_nix_attribute }}
        run: |
          zig_path="$(nix-build '<nixpkgs>' -A "$ZIG_NIX_ATTRIBUTE" --no-out-link)"
          test "$("$zig_path/bin/zig" version)" = "$ZIG_VERSION"
          compiler_description="$(file "$zig_path/bin/zig")"
          echo "$compiler_description"
          grep -q 'dynamically linked' <<<"$compiler_description"
          echo "$zig_path/bin" >> "$GITHUB_PATH"
      - name: Restore the pinned runner for this revision
        id: runner_cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/vopr-cached-runner/vopr.tar.gz
          key: vopr-runner-v1-${{ runner.os }}-${{ runner.arch }}-${{ inputs.head_sha || github.sha }}
      - name: Build campaign runner
        if: steps.runner_cache.outputs.cache-hit != 'true'
        working-directory: zig
        run: |
          python3 "$GITHUB_WORKSPACE/scripts/ci/measure_disk_usage.py" --output "$RUNNER_TEMP/vopr-build-disk.json" --path /mnt/cache --path "$GITHUB_WORKSPACE" -- \
            python3 tools/run_bounded_zig_build.py --max-rss-cap 23622320128 -- build vopr-build -Doptimize=ReleaseSafe --summary all \
            --cache-dir "$VOPR_LOCAL_CACHE_DIR" --global-cache-dir "$VOPR_GLOBAL_CACHE_DIR"
      - name: Retain runner build disk measurements
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-build-disk
          path: ${{ runner.temp }}/vopr-build-disk.json
          if-no-files-found: warn
      - name: Restore executable
        if: steps.runner_cache.outputs.cache-hit == 'true'
        run: |
          mkdir -p zig/zig-out/bin
          tar -C zig/zig-out/bin -xzf "$RUNNER_TEMP/vopr-cached-runner/vopr.tar.gz"
      - name: Identify the exact runner
        id: binary
        run: |
          digest="$(sha256sum zig/zig-out/bin/vopr)"
          echo "sha256=${digest%% *}" >> "$GITHUB_OUTPUT"
      - name: Exercise campaign merge restore and reuse
        run: python3 scripts/ci/zig_vopr_qualification.py --binary zig/zig-out/bin/vopr --output "$RUNNER_TEMP/vopr-qualification"
      - name: Retain qualification diagnostics
        if: always()
        timeout-minutes: 5
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-qualification-diagnostics
          path: |
            ${{ runner.temp }}/vopr-qualification/**/*.json
            ${{ runner.temp }}/vopr-qualification/**/*.log
            ${{ runner.temp }}/vopr-qualification/**/*.voprtrace
            ${{ runner.temp }}/vopr-qualification/**/*.voprquarantine
          retention-days: 90
      - name: Package executable
        if: steps.runner_cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p "$RUNNER_TEMP/vopr-cached-runner"
          tar -C zig/zig-out/bin -czf "$RUNNER_TEMP/vopr-cached-runner/vopr.tar.gz" vopr
      - name: Preserve the runner layout for subsequent campaigns
        if: steps.runner_cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/vopr-cached-runner/vopr.tar.gz
          key: vopr-runner-v1-${{ runner.os }}-${{ runner.arch }}-${{ inputs.head_sha || github.sha }}
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-runner
          path: ${{ runner.temp }}/vopr-cached-runner/vopr.tar.gz
          retention-days: 90
          overwrite: true

  campaign:
    needs: [build, qualify]
    if: inputs.qualification_only != true
    runs-on: arc-antfly-heavy-runtime
    timeout-minutes: 500
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        scenario: [standby, raft, distributed-data, standby-scaling, secrets]
        include:
          - scenario: standby
            histories: 1000
          - scenario: raft
            histories: 1000
          - scenario: distributed-data
            histories: 12
          - scenario: standby-scaling
            histories: 2
          - scenario: secrets
            histories: 1000
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true
      - name: Sample ARC runner disk use
        uses: ./.github/actions/runner-disk-audit
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: vopr-runner
          path: ${{ runner.temp }}/runner
      - run: tar -C "$RUNNER_TEMP/runner" -xzf "$RUNNER_TEMP/runner/vopr.tar.gz"
      - name: Restore merged corpus from earlier runs
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/vopr-corpus-retained
          key: vopr-corpus-v2-${{ github.ref_name }}--${{ matrix.scenario }}--${{ needs.build.outputs.binary_sha256 }}--${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            vopr-corpus-v2-${{ github.ref_name }}--${{ matrix.scenario }}--${{ needs.build.outputs.binary_sha256 }}--
      - name: Run retained-corpus campaign
        timeout-minutes: 240
        working-directory: zig
        env:
          SCENARIO: ${{ matrix.scenario }}
          HISTORY_BUDGET: ${{ inputs.histories }}
          DEFAULT_HISTORY_BUDGET: ${{ matrix.histories }}
          RUN_NUMBER: ${{ github.run_number }}
          REQUIRE_SEED: ${{ inputs.require_seed }}
        run: |
          # Dispatch can supply the string "0", which is truthy in expressions.
          # Normalize the sentinel and the absent scheduled input explicitly.
          if [ -z "$HISTORY_BUDGET" ] || [ "$HISTORY_BUDGET" = "0" ]; then
            HISTORY_BUDGET="$DEFAULT_HISTORY_BUDGET"
          fi
          reuse_args=()
          if [ "${REQUIRE_SEED:-false}" = "true" ]; then reuse_args+=(--require-seed); fi
          status=0
          for shard in 0 1; do
            seed=$((0xa17f5500 + RUN_NUMBER * 65536 + shard))
            policy=bounded-fair
            if [ "$shard" = "1" ]; then policy=adversarial; fi
            python3 ../scripts/ci/zig_vopr_soak.py --binary "$RUNNER_TEMP/runner/vopr" run \
              --scenario "$SCENARIO" --histories "$HISTORY_BUDGET" --seed "$seed" \
              --corpus "$RUNNER_TEMP/vopr-corpus-retained" --output "$RUNNER_TEMP/vopr-run/$shard" \
              --exploration-policy "$policy" "${reuse_args[@]}" || status=1
          done
          exit "$status"
      - name: Retain shard 0 diagnostics first
        if: always()
        timeout-minutes: 5
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-diagnostics-${{ matrix.scenario }}--0
          path: |
            ${{ runner.temp }}/vopr-run/0/run.json
            ${{ runner.temp }}/vopr-run/0/results.*
            ${{ runner.temp }}/vopr-run/0/*.log
            ${{ runner.temp }}/vopr-run/0/*.schedule.json
            ${{ runner.temp }}/vopr-run/0/*.progress.json
          retention-days: 90
          overwrite: true
          if-no-files-found: error
      - name: Retain shard 0 replay traces
        if: always()
        timeout-minutes: 40
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-run-${{ matrix.scenario }}--0
          path: |
            ${{ runner.temp }}/vopr-run/0/*.voprtrace
            ${{ runner.temp }}/vopr-run/0/*.schedule.json
            ${{ runner.temp }}/vopr-run/0/*.flight.json
            ${{ runner.temp }}/vopr-run/0/quarantine
          retention-days: 90
          overwrite: true
          if-no-files-found: error
      - name: Retain shard 1 diagnostics first
        if: always()
        timeout-minutes: 5
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-diagnostics-${{ matrix.scenario }}--1
          path: |
            ${{ runner.temp }}/vopr-run/1/run.json
            ${{ runner.temp }}/vopr-run/1/results.*
            ${{ runner.temp }}/vopr-run/1/*.log
            ${{ runner.temp }}/vopr-run/1/*.schedule.json
            ${{ runner.temp }}/vopr-run/1/*.progress.json
          retention-days: 90
          overwrite: true
          if-no-files-found: error
      - name: Retain shard 1 replay traces
        if: always()
        timeout-minutes: 40
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-run-${{ matrix.scenario }}--1
          path: |
            ${{ runner.temp }}/vopr-run/1/*.voprtrace
            ${{ runner.temp }}/vopr-run/1/*.schedule.json
            ${{ runner.temp }}/vopr-run/1/*.flight.json
            ${{ runner.temp }}/vopr-run/1/quarantine
          retention-days: 90
          overwrite: true
          if-no-files-found: error

  # Native production stress complements the replayable scheduling campaigns.
  # Build once, repeat against those bytes, and keep every case's result.
  production-build:
    if: inputs.diagnostic_run == '' && inputs.qualification_only != true
    runs-on: arc-antfly-heavy
    timeout-minutes: 120
    env:
      ZIG_LOCAL_CACHE_DIR: /mnt/cache/antfly/zig/vopr-soak/${{ github.run_id }}-${{ github.run_attempt }}-restore
      ZIG_GLOBAL_CACHE_DIR: /mnt/cache/antfly/zig/zig-global
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true
      - name: Sample ARC runner disk use
        uses: ./.github/actions/runner-disk-audit
      - name: Prepare evidence directories
        run: |
          mkdir -p "$RUNNER_TEMP/production-e2e-soak/tmp" "$RUNNER_TEMP/production-e2e-soak/reports"
          echo "TMPDIR=$RUNNER_TEMP/production-e2e-soak/tmp" >> "$GITHUB_ENV"
          echo "ANTFLY_E2E_REGRESSION_REPORT_DIR=$RUNNER_TEMP/production-e2e-soak/reports" >> "$GITHUB_ENV"
      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy
      - name: Install build tools and native debugger
        run: sudo apt-get update && sudo apt-get install -y build-essential pkg-config ca-certificates git curl xz-utils file gdb
      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31
        with:
          nix_path: nixpkgs=https://github.com/NixOS/nixpkgs/archive/${{ steps.toolchain.outputs.zig_nixpkgs_revision }}.tar.gz
      - name: Install glibc-linked Zig
        env:
          ZIG_VERSION: ${{ steps.toolchain.outputs.zig_version }}
          ZIG_NIX_ATTRIBUTE: ${{ steps.toolchain.outputs.zig_nix_attribute }}
        run: |
          zig_path="$(nix-build '<nixpkgs>' -A "$ZIG_NIX_ATTRIBUTE" --no-out-link)"
          test "$("$zig_path/bin/zig" version)" = "$ZIG_VERSION"
          compiler_description="$(file "$zig_path/bin/zig")"
          echo "$compiler_description"
          grep -q 'dynamically linked' <<<"$compiler_description"
          echo "$zig_path/bin" >> "$GITHUB_PATH"
      - name: Build production executable and qualify owner publication
        id: production-build
        working-directory: zig
        run: |
          python3 "$GITHUB_WORKSPACE/scripts/ci/measure_disk_usage.py" --output "$RUNNER_TEMP/production-e2e-soak/build-disk.json" --path /mnt/cache --path "$GITHUB_WORKSPACE" -- \
            python3 tools/run_bounded_zig_build.py --max-rss-cap 23622320128 -- build antfly antfly-storage-owner-source-test -Doptimize=ReleaseSafe --summary all \
            2>&1 | tee "$RUNNER_TEMP/production-e2e-soak/build.log"
          sha256sum zig-out/bin/antfly > "$RUNNER_TEMP/production-e2e-soak/binary.sha256"
          tar -C zig-out/bin -czf "$RUNNER_TEMP/production-e2e-soak/antfly.tar.gz" antfly
      - name: Soak compiled-owner restore with concurrent maintenance and status
        working-directory: zig
        run: |
          for iteration in $(seq 1 20); do
            python3 tools/run_bounded_zig_build.py --max-rss-cap 23622320128 -- build antfly-storage-owner-source-test -Doptimize=ReleaseSafe --summary all \
              > "$RUNNER_TEMP/production-e2e-soak/owner-$iteration.log" 2>&1
          done
      - name: Retain production binary and build evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: production-soak-binary
          path: |
            ${{ runner.temp }}/production-e2e-soak/antfly.tar.gz
            ${{ runner.temp }}/production-e2e-soak/binary.sha256
            ${{ runner.temp }}/production-e2e-soak/*.log
          if-no-files-found: error
          retention-days: 30

  production-e2e:
    needs: production-build
    runs-on: arc-antfly-heavy-runtime
    timeout-minutes: 180
    env:
      SKIP_BUILD: '1'
      ANTFLY_E2E_ENV_LOADED: '1'
      ANTFLY_BIN: ${{ github.workspace }}/zig/zig-out/bin/antfly
      ANTFLY_E2E_REGRESSION_WORKERS: '4'
      ANTFLY_E2E_REGRESSION_REPEATS: '25'
      ANTFLY_E2E_PRESERVE_FAILURE_LIMIT: '4'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true
      - name: Sample ARC runner disk use
        uses: ./.github/actions/runner-disk-audit
      - name: Prepare evidence directories
        run: |
          mkdir -p "$RUNNER_TEMP/production-e2e-soak/tmp" "$RUNNER_TEMP/production-e2e-soak/reports"
          echo "TMPDIR=$RUNNER_TEMP/production-e2e-soak/tmp" >> "$GITHUB_ENV"
          echo "ANTFLY_E2E_REGRESSION_REPORT_DIR=$RUNNER_TEMP/production-e2e-soak/reports" >> "$GITHUB_ENV"
      - name: Install native debugger
        run: sudo apt-get update && sudo apt-get install -y gdb
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: production-soak-binary
          path: ${{ runner.temp }}/production-e2e-soak
      - name: Restore and verify the production executable
        id: restore-binary
        working-directory: zig
        run: |
          mkdir -p zig-out/bin
          tar -C zig-out/bin -xzf "$RUNNER_TEMP/production-e2e-soak/antfly.tar.gz"
          sha256sum -c "$RUNNER_TEMP/production-e2e-soak/binary.sha256"
      - name: Soak public overwrite restore with concurrent readers and status
        run: |
          scripts/ci/zig-e2e-regression-loop.sh \
            e2e/antfly/test_backup_restore.py::test_cluster_restore_modes \
            e2e/antfly/test_backup_restore.py::test_cluster_restore_modes_with_concurrent_observers \
            2>&1 | tee "$RUNNER_TEMP/production-e2e-soak/e2e.log"
      - name: Soak cross-shard Autograph resolution, promotion, and hydration
        timeout-minutes: 45
        if: ${{ !cancelled() && steps.restore-binary.outcome == 'success' }}
        env:
          ANTFLY_E2E_REGRESSION_WORKERS: '2'
          ANTFLY_E2E_REGRESSION_REPORT_DIR: ${{ runner.temp }}/production-e2e-soak/autograph
        run: |
          scripts/ci/zig-e2e-autograph-soak.sh \
            2>&1 | tee "$RUNNER_TEMP/production-e2e-soak/autograph.log"
      - name: Soak large catalog control and diagnostic isolation
        timeout-minutes: 45
        if: ${{ !cancelled() && steps.restore-binary.outcome == 'success' }}
        env:
          ANTFLY_E2E_REGRESSION_WORKERS: '2'
          ANTFLY_E2E_REGRESSION_REPORT_DIR: ${{ runner.temp }}/production-e2e-soak/catalog
        run: |
          scripts/ci/zig-e2e-catalog-soak.sh \
            2>&1 | tee "$RUNNER_TEMP/production-e2e-soak/catalog.log"
      - name: Soak concurrent aggregations and transaction session recovery
        timeout-minutes: 45
        if: ${{ !cancelled() && steps.restore-binary.outcome == 'success' }}
        env:
          ANTFLY_E2E_REGRESSION_WORKERS: '2'
          ANTFLY_E2E_REGRESSION_REPORT_DIR: ${{ runner.temp }}/production-e2e-soak/query-transaction
        run: |
          scripts/ci/zig-e2e-query-transaction-soak.sh \
            2>&1 | tee "$RUNNER_TEMP/production-e2e-soak/query-transaction.log"
      - name: Verify complete production soak evidence
        if: ${{ !cancelled() && steps.restore-binary.outcome == 'success' }}
        working-directory: zig
        run: |
          sha256sum -c "$RUNNER_TEMP/production-e2e-soak/binary.sha256"
          python3 - <<'PY'
          import os
          from collections import Counter
          from pathlib import Path
          import xml.etree.ElementTree as ET
          reports = list(Path(os.environ["ANTFLY_E2E_REGRESSION_REPORT_DIR"]).glob("*.xml"))
          assert len(reports) == 200, len(reports)
          cases = [case for report in reports for case in ET.parse(report).getroot().findall(".//testcase")]
          assert len(cases) == 200, len(cases)
          assert Counter(case.attrib["name"] for case in cases) == {
              "test_cluster_restore_modes": 100,
              "test_cluster_restore_modes_with_concurrent_observers": 100,
          }
          assert all(not any(case.find(tag) is not None for tag in ("failure", "error", "skipped")) for case in cases)
          for profile in ("normal", "constrained"):
              reports = list((Path(os.environ["RUNNER_TEMP"]) / "production-e2e-soak/query-transaction" / profile).glob("*.xml"))
              assert len(reports) == 100, ("query-transaction", profile, len(reports))
              cases = [case for report in reports for case in ET.parse(report).getroot().findall(".//testcase")]
              assert Counter(case.attrib["name"] for case in cases) == {
                  "test_aggregations_remain_exact_during_concurrent_inserts": 50,
                  "test_session_transform_savepoint_rollback": 50,
              }, profile
              assert all(not any(case.find(tag) is not None for tag in ("failure", "error", "skipped")) for case in cases), profile
          for profile in ("normal", "constrained"):
              reports = list((Path(os.environ["RUNNER_TEMP"]) / "production-e2e-soak/catalog" / profile).glob("*.xml"))
              assert len(reports) == 50, ("catalog", profile, len(reports))
              cases = [case for report in reports for case in ET.parse(report).getroot().findall(".//testcase")]
              assert Counter(case.attrib["name"] for case in cases) == {
                  "test_large_inventory_uses_bounded_control_and_diagnostic_transfers": 50,
              }, profile
              assert all(not any(case.find(tag) is not None for tag in ("failure", "error", "skipped")) for case in cases), profile
          for profile in ("normal", "constrained"):
              reports = list((Path(os.environ["RUNNER_TEMP"]) / "production-e2e-soak/autograph" / profile).glob("*.xml"))
              assert len(reports) == 100, (profile, len(reports))
              cases = [case for report in reports for case in ET.parse(report).getroot().findall(".//testcase")]
              assert Counter(case.attrib["name"] for case in cases) == {
                  "test_multinode_autograph_resolves_promotes_and_hydrates_entities": 50,
                  "test_multinode_autograph_recovers_after_data_restart": 50,
              }, profile
              assert all(not any(case.find(tag) is not None for tag in ("failure", "error", "skipped")) for case in cases), profile
          PY
      - name: Retain production executable and reports
        if: always()
        timeout-minutes: 20
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: production-e2e-soak
          path: |
            ${{ runner.temp }}/production-e2e-soak
            !${{ runner.temp }}/production-e2e-soak/tmp/**
          retention-days: 30
          if-no-files-found: error
      - name: Retain stopped server roots separately
        if: always()
        timeout-minutes: 10
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: production-e2e-soak-roots
          path: ${{ runner.temp }}/production-e2e-soak/tmp
          retention-days: 30
          if-no-files-found: warn

  cluster-restore:
    needs: production-build
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        profile: [normal, constrained]
    runs-on: arc-antfly-heavy-runtime
    timeout-minutes: 100
    env:
      SKIP_BUILD: '1'
      ANTFLY_E2E_ENV_LOADED: '1'
      ANTFLY_BIN: ${{ github.workspace }}/zig/zig-out/bin/antfly
      ANTFLY_E2E_REGRESSION_WORKERS: '2'
      ANTFLY_E2E_REGRESSION_REPEATS: '25'
      ANTFLY_E2E_PRESERVE_FAILURE_LIMIT: '4'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true
      - name: Sample ARC runner disk use
        uses: ./.github/actions/runner-disk-audit
      - name: Prepare evidence directories
        run: |
          mkdir -p "$RUNNER_TEMP/production-e2e-soak/tmp" "$RUNNER_TEMP/production-e2e-soak/reports"
          echo "TMPDIR=$RUNNER_TEMP/production-e2e-soak/tmp" >> "$GITHUB_ENV"
          echo "ANTFLY_E2E_REGRESSION_REPORT_DIR=$RUNNER_TEMP/production-e2e-soak/reports" >> "$GITHUB_ENV"
      - name: Install native debugger
        run: sudo apt-get update && sudo apt-get install -y gdb
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: production-soak-binary
          path: ${{ runner.temp }}/production-e2e-soak
      - name: Restore and verify the production executable
        id: restore-binary
        working-directory: zig
        run: |
          mkdir -p zig-out/bin
          tar -C zig-out/bin -xzf "$RUNNER_TEMP/production-e2e-soak/antfly.tar.gz"
          sha256sum -c "$RUNNER_TEMP/production-e2e-soak/binary.sha256"
      - name: Soak three by three cluster backup delete and restore
        timeout-minutes: 90
        env:
          ANTFLY_E2E_REGRESSION_PROFILE: ${{ matrix.profile }}
          ANTFLY_E2E_REGRESSION_REPORT_DIR: ${{ runner.temp }}/production-e2e-soak/cluster-restore
        run: |
          python3 scripts/ci/run_e2e_case.py --timeout 4800 --grace 30 scripts/ci/zig-e2e-cluster-restore-soak.sh \
            2>&1 | tee "$RUNNER_TEMP/production-e2e-soak/cluster-restore.log"
        # Reserve ten minutes for process shutdown and artifact collection.
        # The inner per-case supervisors keep their separate ten-minute budget.
      - name: Verify complete cluster restore evidence
        env:
          SOAK_PROFILE: ${{ matrix.profile }}
        run: |
          python3 - <<'PYVERIFY'
          import os
          from collections import Counter
          from pathlib import Path
          import xml.etree.ElementTree as ET
          root = Path(os.environ["RUNNER_TEMP"]) / "production-e2e-soak/cluster-restore" / os.environ["SOAK_PROFILE"]
          reports = list(root.glob("*.xml"))
          assert len(reports) == 100, len(reports)
          cases = [case for report in reports for case in ET.parse(report).getroot().findall(".//testcase")]
          assert Counter(case.attrib["name"] for case in cases) == {
              "test_three_by_three_cluster_backup_restore_through_metadata_public_api": 50,
              "test_backup_restore_discovers_leader_past_stalled_status": 50,
          }
          assert all(not any(case.find(tag) is not None for tag in ("failure", "error", "skipped")) for case in cases)
          PYVERIFY
      - name: Retain cluster restore reports
        if: always()
        timeout-minutes: 10
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: cluster-restore-${{ matrix.profile }}
          path: |
            ${{ runner.temp }}/production-e2e-soak
            !${{ runner.temp }}/production-e2e-soak/tmp/**
          retention-days: 30
          if-no-files-found: error
      - name: Retain stopped server roots separately
        if: always()
        timeout-minutes: 10
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: cluster-restore-${{ matrix.profile }}-roots
          path: ${{ runner.temp }}/production-e2e-soak/tmp
          retention-days: 30
          if-no-files-found: warn

  corpus:
    needs: [build, qualify, campaign]
    if: always() && needs.build.result == 'success' && needs.qualify.result == 'success' && inputs.qualification_only != true
    runs-on: arc-antfly-heavy-runtime
    timeout-minutes: 600
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true
      - name: Sample ARC runner disk use
        uses: ./.github/actions/runner-disk-audit
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: vopr-runner
          path: ${{ runner.temp }}/runner
      - run: tar -C "$RUNNER_TEMP/runner" -xzf "$RUNNER_TEMP/runner/vopr.tar.gz"
      - name: Download standby shards
        if: always() && !cancelled()
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: vopr-run-standby--*
          path: ${{ runner.temp }}/vopr-shards/standby
      - name: Replay and merge standby corpus
        id: merge_standby
        if: always() && !cancelled()
        timeout-minutes: 120
        working-directory: zig
        run: |
          python3 ../scripts/ci/zig_vopr_soak.py --binary "$RUNNER_TEMP/runner/vopr" merge \
            --inputs "$RUNNER_TEMP/vopr-shards/standby" --output "$RUNNER_TEMP/vopr-corpus/standby" \
            --retained "$RUNNER_TEMP/vopr-corpus-retained"
      - name: Retain standby corpus diagnostics
        if: always()
        timeout-minutes: 5
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-corpus-diagnostics-standby
          path: |
            ${{ runner.temp }}/vopr-corpus/standby/*.json
            ${{ runner.temp }}/vopr-corpus/standby/*.log
          retention-days: 90
          overwrite: true
          if-no-files-found: error
      - name: Retain standby merged corpus
        if: always()
        timeout-minutes: 40
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-corpus-standby
          path: ${{ runner.temp }}/vopr-corpus/standby
          retention-days: 90
          overwrite: true
          if-no-files-found: error
      - name: Publish standby corpus for the next scheduled run
        if: always() && steps.merge_standby.outcome == 'success'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/vopr-corpus-retained
          key: vopr-corpus-v2-${{ github.ref_name }}--standby--${{ needs.build.outputs.binary_sha256 }}--${{ github.run_id }}-${{ github.run_attempt }}
      - name: Clear standby corpus before the next scenario
        if: always()
        run: rm -rf "$RUNNER_TEMP/vopr-corpus-retained"
      - name: Download raft shards
        if: always() && !cancelled()
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: vopr-run-raft--*
          path: ${{ runner.temp }}/vopr-shards/raft
      - name: Replay and merge raft corpus
        id: merge_raft
        if: always() && !cancelled()
        timeout-minutes: 120
        working-directory: zig
        run: |
          python3 ../scripts/ci/zig_vopr_soak.py --binary "$RUNNER_TEMP/runner/vopr" merge \
            --inputs "$RUNNER_TEMP/vopr-shards/raft" --output "$RUNNER_TEMP/vopr-corpus/raft" \
            --retained "$RUNNER_TEMP/vopr-corpus-retained"
      - name: Retain raft corpus diagnostics
        if: always()
        timeout-minutes: 5
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-corpus-diagnostics-raft
          path: |
            ${{ runner.temp }}/vopr-corpus/raft/*.json
            ${{ runner.temp }}/vopr-corpus/raft/*.log
          retention-days: 90
          overwrite: true
          if-no-files-found: error
      - name: Retain raft merged corpus
        if: always()
        timeout-minutes: 40
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-corpus-raft
          path: ${{ runner.temp }}/vopr-corpus/raft
          retention-days: 90
          overwrite: true
          if-no-files-found: error
      - name: Publish raft corpus for the next scheduled run
        if: always() && steps.merge_raft.outcome == 'success'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/vopr-corpus-retained
          key: vopr-corpus-v2-${{ github.ref_name }}--raft--${{ needs.build.outputs.binary_sha256 }}--${{ github.run_id }}-${{ github.run_attempt }}
      - name: Clear raft corpus before the next scenario
        if: always()
        run: rm -rf "$RUNNER_TEMP/vopr-corpus-retained"
      - name: Download distributed-data shards
        if: always() && !cancelled()
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: vopr-run-distributed-data--*
          path: ${{ runner.temp }}/vopr-shards/distributed-data
      - name: Replay and merge distributed-data corpus
        id: merge_distributed_data
        if: always() && !cancelled()
        timeout-minutes: 120
        working-directory: zig
        run: |
          python3 ../scripts/ci/zig_vopr_soak.py --binary "$RUNNER_TEMP/runner/vopr" merge \
            --inputs "$RUNNER_TEMP/vopr-shards/distributed-data" --output "$RUNNER_TEMP/vopr-corpus/distributed-data" \
            --retained "$RUNNER_TEMP/vopr-corpus-retained"
      - name: Retain distributed-data corpus diagnostics
        if: always()
        timeout-minutes: 5
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-corpus-diagnostics-distributed-data
          path: |
            ${{ runner.temp }}/vopr-corpus/distributed-data/*.json
            ${{ runner.temp }}/vopr-corpus/distributed-data/*.log
          retention-days: 90
          overwrite: true
          if-no-files-found: error
      - name: Retain distributed-data merged corpus
        if: always()
        timeout-minutes: 40
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-corpus-distributed-data
          path: ${{ runner.temp }}/vopr-corpus/distributed-data
          retention-days: 90
          overwrite: true
          if-no-files-found: error
      - name: Publish distributed-data corpus for the next scheduled run
        if: always() && steps.merge_distributed_data.outcome == 'success'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/vopr-corpus-retained
          key: vopr-corpus-v2-${{ github.ref_name }}--distributed-data--${{ needs.build.outputs.binary_sha256 }}--${{ github.run_id }}-${{ github.run_attempt }}
      - name: Clear distributed-data corpus before the next scenario
        if: always()
        run: rm -rf "$RUNNER_TEMP/vopr-corpus-retained"
      - name: Download standby-scaling shards
        if: always() && !cancelled()
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: vopr-run-standby-scaling--*
          path: ${{ runner.temp }}/vopr-shards/standby-scaling
      - name: Replay and merge standby-scaling corpus
        id: merge_standby_scaling
        if: always() && !cancelled()
        timeout-minutes: 120
        working-directory: zig
        run: |
          python3 ../scripts/ci/zig_vopr_soak.py --binary "$RUNNER_TEMP/runner/vopr" merge \
            --inputs "$RUNNER_TEMP/vopr-shards/standby-scaling" --output "$RUNNER_TEMP/vopr-corpus/standby-scaling" \
            --retained "$RUNNER_TEMP/vopr-corpus-retained"
      - name: Retain standby-scaling corpus diagnostics
        if: always()
        timeout-minutes: 5
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-corpus-diagnostics-standby-scaling
          path: |
            ${{ runner.temp }}/vopr-corpus/standby-scaling/*.json
            ${{ runner.temp }}/vopr-corpus/standby-scaling/*.log
          retention-days: 90
          overwrite: true
          if-no-files-found: error
      - name: Retain standby-scaling merged corpus
        if: always()
        timeout-minutes: 40
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-corpus-standby-scaling
          path: ${{ runner.temp }}/vopr-corpus/standby-scaling
          retention-days: 90
          overwrite: true
          if-no-files-found: error
      - name: Publish standby-scaling corpus for the next scheduled run
        if: always() && steps.merge_standby_scaling.outcome == 'success'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/vopr-corpus-retained
          key: vopr-corpus-v2-${{ github.ref_name }}--standby-scaling--${{ needs.build.outputs.binary_sha256 }}--${{ github.run_id }}-${{ github.run_attempt }}
      - name: Clear standby-scaling corpus before the next scenario
        if: always()
        run: rm -rf "$RUNNER_TEMP/vopr-corpus-retained"
      - name: Download secrets shards
        if: always() && !cancelled()
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: vopr-run-secrets--*
          path: ${{ runner.temp }}/vopr-shards/secrets
      - name: Replay and merge secrets corpus
        id: merge_secrets
        if: always() && !cancelled()
        timeout-minutes: 120
        working-directory: zig
        run: |
          python3 ../scripts/ci/zig_vopr_soak.py --binary "$RUNNER_TEMP/runner/vopr" merge \
            --inputs "$RUNNER_TEMP/vopr-shards/secrets" --output "$RUNNER_TEMP/vopr-corpus/secrets" \
            --retained "$RUNNER_TEMP/vopr-corpus-retained"
      - name: Retain secrets corpus diagnostics
        if: always()
        timeout-minutes: 5
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-corpus-diagnostics-secrets
          path: |
            ${{ runner.temp }}/vopr-corpus/secrets/*.json
            ${{ runner.temp }}/vopr-corpus/secrets/*.log
          retention-days: 90
          overwrite: true
          if-no-files-found: error
      - name: Retain secrets merged corpus
        if: always()
        timeout-minutes: 40
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vopr-corpus-secrets
          path: ${{ runner.temp }}/vopr-corpus/secrets
          retention-days: 90
          overwrite: true
          if-no-files-found: error
      - name: Publish secrets corpus for the next scheduled run
        if: always() && steps.merge_secrets.outcome == 'success'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/vopr-corpus-retained
          key: vopr-corpus-v2-${{ github.ref_name }}--secrets--${{ needs.build.outputs.binary_sha256 }}--${{ github.run_id }}-${{ github.run_attempt }}
      - name: Clear secrets corpus before the next scenario
        if: always()
        run: rm -rf "$RUNNER_TEMP/vopr-corpus-retained"
